name: Get body

on:
    pull_request:

jobs:
    comment:
        runs-on: ubuntu-latest
        steps:
            - name: Read body
              id: version
              run: |
                azure_task = echo "${{ github.event.pull_request.body }}" | grep -oP AB[0-9]+
                echo $azure_task
